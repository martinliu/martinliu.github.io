# TEMPORARILY DISABLED - 2025-08-21
# Will be re-enabled after testing new deployment workflows

# Original purpose: CodeQL security analysis
# To re-enable, restore the original content and update the name

name: DISABLED-CodeQL

# Disable all triggers
on: []

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is temporarily disabled
        run: echo "CodeQL analysis is temporarily disabled during deployment workflow testing."

# Original workflow content (for reference):
# name: "CodeQL"
# 
# on:
#   push:
#     branches: [ master ]
#   pull_request:
#     branches: [ master ]
#   schedule:
#     - cron: '40 19 * * 3'
# 
# jobs:
#   analyze:
#     name: Analyze
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         language: [ 'javascript', 'html']
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4
#     - name: Initialize CodeQL
#       uses: github/codeql-action/init@v3
#       with:
#         languages: ${{ matrix.language }}
#     - name: Autobuild
#       uses: github/codeql-action/autobuild@v1
#     - name: Perform CodeQL Analysis
#       uses: github/codeql-action/analyze@v1
