# DISABLED - 2025-08-21
# This workflow has been replaced by deploy-2-page-improved.yml
# Keeping this file for reference

# Original workflow name: CI - build and test on gh-pages
# Original purpose: Build preview version on github pages

# To re-enable, uncomment the following lines and comment out the above

name: DISABLED-deploy-2-page

# Disable all triggers
on: []

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "This workflow has been disabled. Use deploy-2-page-improved.yml instead."